---
import projects from "../../config/projects.json";
---

<section>
  <h2 class="font-bold text-4xl mt-9 text-center">Projects</h2>
  {
    projects.map((project) => (
        <div class="flex flex-col mb-2 text-center md:text-left md:flex-row">
          <div class="flex-1 gap-y-3 p-3">
            <h2 class="font-bold text-2xl my-4"> {project.title}</h2>
            <p class="my-4">{project.description}</p>
            <button class="bg-indigo-500 px-3 py-2 hover:bg-indigo-400 ">More info</button>
          </div>
          <div class="flex-1">
            <img src={project.img} alt="" class="w-72 m-auto"/>
          </div>
        </div>
    ))
  }
</section>
